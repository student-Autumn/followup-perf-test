-
  feature: 调查问卷管理
  story: 查询题目列表
  title: 根据问卷ID查询题目列表成功
  request:
    method: get
    url: ${evn(base_url)}/survey/questions/${read_extract(realSurveyId)}
  validate:
    eq:
      - 状态码为200: [status_code, 200]
      - 响应成功: ["json[success]", true]
