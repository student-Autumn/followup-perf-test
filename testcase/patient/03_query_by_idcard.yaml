-
  feature: 病人管理
  story: 根据身份证号查询病人信息
  title: 根据身份证号查询病人信息成功
  request:
    method: get
    url: ${evn(base_url)}/patient/by-idcard/${read_extract(realIdNo)}
  validate:
    eq:
      - 状态码为200: [status_code, 200]
      - 响应成功: ["json[success]", true]
