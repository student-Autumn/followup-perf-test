-
  feature: 病人管理
  story: 删除病人信息
  title: 删除病人信息成功
  request:
    method: post
    url: ${evn(base_url)}/patient/delete/${read_extract(patientId)}
  validate:
    eq:
      - 状态码为200: [status_code, 200]
      - 响应成功: ["json[success]", true]
      - 删除返回true: ["json[data]", true]
