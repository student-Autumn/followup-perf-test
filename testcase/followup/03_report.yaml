-
  feature: 随访管理
  story: 打印随访报告
  title: 根据随访记录ID打印报告成功
  request:
    method: get
    url: ${evn(base_url)}/followup/record/report/${read_extract(realReportId)}
  validate:
    eq:
      - 状态码为200: [status_code, 200]
      - 响应成功: ["json[success]", true]
